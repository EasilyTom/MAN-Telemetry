#Create a tkinter window that displays one string of text
import tkinter as tk


# Create the main window with size 200x200
window = tk.Tk(baseName= "CAN Dashboard")
window.geometry("500x500")
window.title("CAN Dashboard")
window.mainloop()
